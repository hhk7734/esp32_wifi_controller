name: esp32_wifi_controller
description: ESP32 WiFi Controller

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^6.1.0
  equatable: ^1.2.5
  flutter_bloc: ^6.1.0
  formz: ^0.3.2

  esp32_repository:
    path: packages/esp32_repository

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
